/* host.c - y1cc.c built for the Mac: the host table sizes, then the compiler (its I/O is host_io.c).
   cc -std=c89 -Wall -Wextra -pedantic -o y1cc host.c host_io.c      (the Makefile) */
#include "limits_host.h"
#include "y1cc.c"
