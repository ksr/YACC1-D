# upstream — RC/asm 2.2 as received

Michael H. Riley's RC/asm v2.2, unmodified (`rcasm-2.2/`): sources, `.def` tables for 1802/1805/4004/6502/8080/z80/z8/
chip8/pic24, `rclink`, `makeswitch`, and prebuilt x86_64 binaries `rcasm`, `rclink`, `mk`. Reference for what the
YACC1 port changed (`diff -r rcasm-2.2 ..`). The manual `asm.txt`/`asm.doc` is identical to the one beside the port.
