*Converted from `dev notes.rtf` (saved 2025-03-06) by `tools/rtf_to_md.py`; the .rtf is the original.*

Verify opcode using putBusAtMemReg 
    removed clear -vma

Add halt indicator led to sequencer board
Expose code-counter-reset either on bus or as a pin to support external clock control

Memory

Add leds for each of 3 chips CS

Add keyhole optimizer looking for duplicate lines in micro code
