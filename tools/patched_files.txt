# Migrated files that were deliberately EDITED in YACC1-D (so they no longer match their YACCS original).
# One path per line, then a tab and the reason. tools/audit_tree.py accepts a differing hash for these;
# tools/migrate_purge.py never reverts them; tools/migrate_run.py never overwrites them.
software/disassembler/disasm2/main.c	2026-09-20: re-enabled the yaccsignaldata2.h include, printWave() void; test.123 found relative to the executable; optional opcode arguments; two debug calls removed; unused locals removed, explicit initializer braces
software/emulator/main.c	2026-09-20: -m (and the no-argument default) load firmware/basic/basic.img + firmware/monitor/monitor.img, located relative to the executable (works from Finder / any cwd); six unused locals removed; 2026-09-22: -x scripted mode for the compiler tests (no load/dump chatter, stdout flushed, HALT exits with the instruction count on stderr, no raw tty mode); BRVR = indirect jump through the word at Rn with Rn += 2 and JSRUR = PC <- Rn, both as the microcode does them (docs/isa/MICROCODE-REVIEW-NOTES.md section 3; the old BRVR never branched and JSRUR swapped the bytes, so the monitor's G command and T menu never worked on the emulator; BASIC uses neither); BRUR Rn ($AD) = PC <- Rn implemented; CompactFlash card model on ports P8/P9 (software/cfmodel.h, -c image); -l N instruction limit for scripted runs of code that never HALTs; 2026-09-23: the CF model moved to ports P4/P5 (software/cfmodel.h; the interface moved onto the I/O card v2.0, decoded by its IC5); 2026-09-24: CF ports back to P8/P9 (the I/O card v2.0 plan was dropped; the CF interface goes on the memory card, I/O-mapped) - the files are the pre-23B ones again, ROM = the burned d2d7b027; 2026-09-24: LDZ/STZ ($80-$8F, the word at R6.hi:d), ADDIW ($C0-$C7), SHL16 ($C8-$CF) as the microcode does them (ACC = the result's high byte, carry out of bit 15); the OUTVR (bad opcode) and LDTVR/STTVR cases they replace removed
firmware/microcode/ucode-generator2/controlLine.c	2026-09-20: outputs (test.123/.hex/.hexz) written beside the executable, not in the cwd (Finder double-click); unused locals removed
firmware/microcode/ucode-generator2/main.c	2026-09-20: keeps argv[0] for controlLine.c's exe_relative(); unused locals removed
software/ubasic-c/ubasic-master/use-ubasic.c	2026-09-20: interactive mode exits at end of input instead of looping forever; unused locals removed, the OLD-main sample program under #ifdef OLD
software/assembler/asm.c	2026-09-20: usage message when no source is given, clean error when the .def is missing (both crashed before); unused local removed; 2026-09-24: make_label() stops with a clear error when the label table is full or a label is longer than 29 characters (both used to overwrite memory: 3,817 labels crashed it); the symbol-sort swap buffer l[20] -> l[30] (overflowed on 20-29 character labels) and the sorts keep labelf with its label
software/assembler/asmcmds.c	2026-09-20: parentheses in the quote/comma loop condition; explicit (int) cast in the abs() range check (both warning fixes, no behaviour change); unused locals class/i/m2/BPtr/buffer removed; 2026-09-22: DS (\B) flushes the pending Intel-hex record like ORG does - bytes after a DS were appended to the record that started before it and landed at the wrong address (the firmware never uses DS: images unchanged, make check proves it); 2026-09-24: EQU (\E) sets the label's value in pass 1 as well (needed now that pass 1 resolves backward references; before, pass 1 saw an EQU label's address)
software/assembler/support.c	2026-09-20: #include <stdlib.h> for atoi(), old K&R 'int atoi();' declaration removed; unused locals mode/i/e/sign removed; 2026-09-24: buildTokens() skips spaces/tabs inside an operand (nothing advanced past them, so 'DW A, B' looped forever); find_label_number() initialises f (a missing label returned garbage); 2026-09-24: find_label() returns an already-defined label's value in pass 1 too (was always 1), so ORG/DS/EQU operands over backward references place what follows correctly (y1cc --xisa aligns its page with DS (256-(zpad).0)&255); forward references still 1
software/assembler/mstrings.c	2026-09-20: unused locals tmpr/dest removed
firmware/microcode/ucode-generator2/code.h	2026-09-20: bitOn/bitOff prototypes (were K&R declarations)
software/ubasic-c/ubasic-master/tokenizer.c	2026-09-20: (const char *)/(char *) casts on the unsigned-char token buffers passed to strlen/strcpy/memcpy (no behaviour change); unused locals removed; debug-only src/dest marked used
software/ubasic-c/ubasic-master/ubasic.c	2026-09-20: dangling index_free() declaration commented out (never defined; calls sit under #ifdef unused)
firmware/microcode/yaccsignaldata2.h	2026-09-20: every table entry wrapped in braces (identical data; silences -Wmissing-braces in the tools that include it)
embedded/bus-tester/bus-driver/bus-driver.ino	2026-09-20: compiler-warning cleanup (arduino-cli --warnings all, 0 warnings): writeDataLo/setCntlPin void, return after doError in opLookUp, (int) casts on the char chip index, unused tmp buffers removed; 2026-09-21: RDBLK/WRBLK block memory commands, startup banner "bus-driver blocks-1", BAUD define
embedded/bus-tester/bus-monitor/bus-monitor.ino	2026-09-20: compiler-warning cleanup (arduino-cli --warnings all, 0 warnings): unused locals chip/pin/tmp removed
embedded/bus-tester/bus-test/bus-test.ino	2026-09-20: compiler-warning cleanup (arduino-cli --warnings all, 0 warnings): unused local mask removed
embedded/bus-tester/led-switch-test/led-switch-test.ino	2026-09-20: compiler-warning cleanup (arduino-cli --warnings all, 0 warnings): unused locals tmp/mask/j removed
embedded/sequencer-card/dumpram/dumpram.ino	2026-09-20: compiler-warning cleanup (arduino-cli --warnings all, 0 warnings): readInstruction void, errorMsg marked used, dead block in loop() and unused tmp removed
embedded/sequencer-card/deprecated/sequencer3/EEPROM.ino	2026-09-20: compiler-warning cleanup (arduino-cli --warnings all, 0 warnings): unused 'int res =' results dropped (calls kept)
embedded/sequencer-card/deprecated/sequencer3/IO.ino	2026-09-20: compiler-warning cleanup (arduino-cli --warnings all, 0 warnings): writeData void, unused tmp removed
embedded/sequencer-card/deprecated/sequencer3/Sequencer3.ino	2026-09-20: compiler-warning cleanup (arduino-cli --warnings all, 0 warnings): errorMsg marked used, unused j removed
embedded/sequencer-card/deprecated/sequencer3/download.ino	2026-09-20: compiler-warning cleanup (arduino-cli --warnings all, 0 warnings): return false after doError in waitInstructionBegin, unused instruction/tmp removed, checksum tables marked unused
embedded/sequencer-card/deprecated/sequencer3/uCodeRAM.ino	2026-09-20: compiler-warning cleanup (arduino-cli --warnings all, 0 warnings): readInstruction void, unused i removed
embedded/sequencer-card/deprecated/test-eeprom-2020-12/mem/mem.ino	2026-09-20: compiler-warning cleanup (arduino-cli --warnings all, 0 warnings): (int) cast on strlen compare, unused tmp/instruction removed
embedded/command-sender/command_sender_8/command_sender_8.pde	2026-09-20: builds under Processing 4 again (java.io.* -> nothing, 'frame' -> null parent); 14 unused imports removed, saveLabel parameter renamed (shadowed the global index), empty while bodies made explicit, cancelled port dialog no longer falls through; zero warnings under tools/verify_processing.py
firmware/monitor/monitor.asm	2026-09-22: G command uses JSRUR R7 + BR cmdloop instead of BRVR R7 (BRVR is an indirect jump through the word at the address and pushes no return: the microcode's branch() fetches the target through the register), help text updated; same day: the T-menu bench tests removed (1,062 bytes; git history / tests/assembler/history-2020), CompactFlash driver (ports P8 select / P9 data: cfinit/cfread/cfwrite, bounded waits, const), O command boots LBA 1.. (OSCNT from the P8XFS boot block) to $1000 and JSRURs it, BIOS vectors CFINIT $FFEC CFREAD $FFF0 CFWRITE $FFF4 CONST $FFF8, variables CFLBA0..2 $0F10, ARGBUF $0F40; = the monitor to burn next; 2026-09-23: ':' Intel-hex loader (hexload, lderr \$0F01, LOADMODE) and the build date in the banner; 2026-09-23 evening: the CF driver moved from P8/P9 to P4/P5 (the CF interface folded onto the I/O card v2.0, whose IC5 decodes P4/P5 on its spare Y4/Y5), banner ROM 2026-09-23B; 2026-09-24: CF ports back to P8/P9 (the I/O card v2.0 plan was dropped; the CF interface goes on the memory card, I/O-mapped) - the files are the pre-23B ones again, ROM = the burned d2d7b027
firmware/monitor/monitor.img	2026-09-22: rebuilt from the edited monitor.asm (G fix, then the CF driver + O boot command); the 2021 build that is on the chip = firmware/rom/eprom-captured-2026-09-18.* ($F000 half) and git history; 2026-09-23 evening: rebuilt for the CF ports P4/P5 (banner ROM 2026-09-23B); 2026-09-24: CF ports back to P8/P9 (the I/O card v2.0 plan was dropped; the CF interface goes on the memory card, I/O-mapped) - the files are the pre-23B ones again, ROM = the burned d2d7b027
firmware/monitor/monitor.lst	2026-09-22: listing of that rebuild; 2026-09-23: rebuilt with the UARTINNE vector, then the : loader and dated banner (burned, MD5 d2d7b027), then the CF ports P4/P5 (banner ROM 2026-09-23B, MD5 a9fefd4a, not burned); 2026-09-24: CF ports back to P8/P9 (the I/O card v2.0 plan was dropped; the CF interface goes on the memory card, I/O-mapped) - the files are the pre-23B ones again, ROM = the burned d2d7b027
firmware/rom/shipped/rom	2026-09-22: basic.img (unchanged) + the rebuilt monitor.img (G fix, CF driver, O boot) = the image TO BURN; the chip still holds the 2021 build (eprom-captured-2026-09-18.bin/.hex) until it is reburned; 2026-09-23: rebuilt with the UARTINNE vector, then the : loader and dated banner (burned, MD5 d2d7b027), then the CF ports P4/P5 (banner ROM 2026-09-23B, MD5 a9fefd4a, not burned); 2026-09-24: CF ports back to P8/P9 (the I/O card v2.0 plan was dropped; the CF interface goes on the memory card, I/O-mapped) - the files are the pre-23B ones again, ROM = the burned d2d7b027
firmware/microcode/ucode-generator2/branch.c	2026-09-22: BRUR Rn added (opcode $AD, 2 bytes, register in the operand byte): JSRUR's operand/register-to-branch-register steps without the stack push, then the BR-family PC load; H-2 fix: branch() clears -AC-RD before -BRANCH-RD for the AC-sourced conditions; H-1 fix: PUSHR releases -REG-RD-HI/LO, -REG-FUNC-RD and -HL-SWAP before each stack write (both reproduced and verified on software/ucemu)
software/opcodes.h	2026-09-22: OPCODE_AD -> BRUR 0xAD; 2026-09-24: OUTVR 0x80 -> LDZ 0x80 + STZ 0x88, LDTVR 0xC0 -> ADDIW 0xC0, STTVR 0xC8 -> SHL16 0xC8 (the replaced ones never had microcode)
software/assembler/yacc1.def	2026-09-22: BRUR \{regs} -> AD \1; P8=9 typo fixed to P8=8 (no firmware uses P8); 2026-09-23: IADDR encodes FE + word (was FE|1, which ORed the operand's bit 0 into the opcode and could assemble as INT $FF); 2026-09-24: OUTVR/LDTVR/STTVR (no microcode) replaced by LDZ \{regs},\B (80|1), STZ (88|1), ADDIW \{regs},\W (C0|1 hi lo), SHL16 \{regs} (C8|1)
firmware/microcode/ucode-generator2/test.hex	2026-09-22: regenerated with BRUR at $AD, the H-2 fix ($A1 $A2 $AB $AC) and the H-1 fix ($07..$0F); 14 records differ from the image the sequencer EEPROM holds until it is reloaded; 2026-09-24: regenerated with LDZ/STZ ($80-$8F) and ADDIW/SHL16 ($C0-$CF): 32 records differ from the image the sequencer EEPROM holds (cache) until it is reloaded
firmware/microcode/ucode-generator2/test.hexz	2026-09-22: regenerated (loader format) with BRUR; 2026-09-24: regenerated (LDZ/STZ/ADDIW/SHL16)
firmware/microcode/ucode-generator2/test.123	2026-09-22: regenerated (listing) with BRUR; 2026-09-24: regenerated (LDZ/STZ/ADDIW/SHL16)
firmware/microcode/ucode-generator2/cache	2026-09-22: rewritten by tools/ucode_send.py --all (the loader's record of what the sequencer EEPROM holds = test.hex with BRUR + H-1/H-2)
firmware/microcode/ucode-generator2/accumulator.c	2026-09-23: aluOp() clears the ALU's SHIFT-OUT flip-flop (SHIFT_LOAD + -SR-LD) before every add/subtract (the carry latches CO/BO OR SHIFT-OUT; a stale shift-out corrupted multi-byte arithmetic on the machine); 2026-09-24: ADDIW Rn,#w ($C0-$C7) and SHL16 Rn ($C8-$CF) added (helpers accFromRegByte/releaseRegRead/regByteFromAcc); no existing record changes
software/assembler/header.h	2026-09-24: MAX_LABELS 1000 -> 8192 (y1cc.c compiled for the YACC1 needs 3,817), LABEL_CHARS 29 for the length check
firmware/microcode/ucode-generator2/register.c	2026-09-24: LDR's and STR's second halves moved into loadRegFromIR()/storeRegAtIR() (the same lines: $E8-$F7 byte-identical) and shared with the new LDZ Rn,d ($80-$87) / STZ Rn,d ($88-$8F), whose operand address zpageAddress() builds in IR from R6's high byte and the offset byte
firmware/microcode/ucode-generator2/CodeGen.h	2026-09-24: #define ZP 6 (the page register of LDZ/STZ)
